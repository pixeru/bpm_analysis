# Main app dependencies (Blender BPM tool scripts use Blender's built-in Python/bpy; no pip install needed)
# Version ranges below support both Python 3.10 and 3.11+ (pip installs the latest compatible with your interpreter).
dash>=3.3.0
librosa>=0.11.0
numpy>=2.0
pandas>=2.3.0
plotly>=6.5.0
kaleido>=0.2.1
pydub>=0.25.1
pyPCG>=0.0.4
pyperclip>=1.11.0
scipy>=1.14
ttkbootstrap>=1.19.2

# Additional runtime dependencies used in the project
matplotlib
PyWavelets
soxr
